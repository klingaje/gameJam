// Checks if the speed is above 0
if (speed > 0)
{
	// Sets the index to run animation.
	sprite_index = spr_hero_run_1;
}
else
{
	// Sets the index to idle animation.
	sprite_index = spr_hero_idle_1;
}